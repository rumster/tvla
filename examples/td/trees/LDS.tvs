// Tree with root "root"
%n = {u, us, sent}
%p = {
	sm = {us:1/2}
	root = {u}
	sent = {sent}
	left = {u->us:1/2, us->us:1/2}
	right = {u->us:1/2, us->us:1/2}
	down = {u->us:1/2,us->us:1/2}
	downStar = {u->u, u->us, us->us:1/2, sent->sent}
  r[root] = {u,us}
	r[sent] = {sent}
}
