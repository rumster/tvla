// A Tree with a root "root1"
%n = {n_root1}
%p = {
	sm = {n_root1:1/2}
	root1 = {n_root1:1/2}
	left = {
                n_root1 ->n_root1:1/2 
               }
        right = {
                n_root1 ->n_root1:1/2 
               }
        down = {
                n_root1 ->n_root1:1/2
               }
        downStar = {
                n_root1 ->n_root1:1/2
               }      
	r[root1] = {n_root1}
}
